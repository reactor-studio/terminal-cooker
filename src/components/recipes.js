const { h, Component, Color } = require('ink');

class Recipes extends Component {
	render(props) {
		return (
			<div>
				<Color>This are recipes</Color>
			</div>
		);
	}
}

module.exports = Recipes;
